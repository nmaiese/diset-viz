# Il giro di canary

Prima di cambiare **modello, prompt, skill, rubrica, hook o permessi** degli
agenti della catena, si misura. Un cambio di questi non rompe mai un test:
cambia il giudizio, e il giudizio non ha una suite. La rete e' questa
procedura, e va fatta PRIMA di fondere il cambio, perche' dopo il primo giro
di Routine il nuovo comportamento e' gia' in pagina.

## 1. Le eval congelate

```bash
python3 evals/score_eval.py --self-test     # il metro e' integro
python3 evals/run_eval.py writer            # poi si fa girare l'agente sul compito
python3 evals/run_eval.py reviewer
python3 evals/run_eval.py verifier
python3 evals/run_eval.py admissions        # il giudizio di triage: quali fonti/indicatori entrano
```

Ogni eval stampa il compito da dare all'agente NUOVO (con il modello o il
prompt candidato) e `score_eval.py` misura il risultato. Il confronto e' con
l'ultimo punteggio annotato qui sotto: un punteggio che scende e' un no, non
un dettaglio. L'eval `admissions` misura il ruolo piu' irreversibile (cosa entra
in una pagina pubblica): la sua metrica di testa e' la **precision sugli
approvati**, perche' una falsa approvazione e' l'errore che nessuno a valle
rivisita.

## 2. Il canary set

Dieci indicatori fissi, scelti perche' coprono le famiglie e i casi che hanno
gia' fatto male (definizione ingannevole, due livelli, contextual, esterno):

| codice | perche' e' nel set |
| --- | --- |
| `ter-178` | il riferimento delle eval, serie lunga e regolare |
| `ter-402` | la definizione che ha gia' ingannato (titolari donne, non imprese) |
| `ter-72` | il perimetro nascosto nel titolo (addetti, non imprese) |
| `ter-60` | la rottura di serie non dichiarata |
| `ter-105` | il tasso di turisticita', il link canonico degli esempi |
| `bes-01SAL001` | due livelli territoriali, articolo per livello |
| `bes-10AMB008` | cifre provinciali, che nessuna guardia controlla |
| `dem-OLDAGEDEPR` | il contextual da manuale (dipendenza anziani) |
| `eur-rd_e_gerdreg` | famiglia Eurostat, fonte e licenza diverse |
| `ims-...` (uno qualsiasi della famiglia) | la famiglia con la definizione `scoperto` |

Sul cambio candidato: un giro di writer su due canary e un giro di reviewer su
altri due, **in dry-run** (senza commit ne' pull request), letti a mano contro
il brief. Se il testo regge la lettura e le eval non scendono, il cambio passa.

## 3. Annotare l'esito

In coda a questo file, una riga per giro: data, che cosa cambiava, punteggi
delle tre eval, verdetto. La versione del runtime e il modello di ogni run di
produzione stanno gia' nel diario (`claude_code_version` e `model` in
`data/pipeline/runs/`), quindi una regressione osservata dopo il fatto si
data e si attribuisce da li'.

## Esiti

| data | cambio | writer | reviewer | verifier | verdetto |
| --- | --- | --- | --- | --- | --- |
| 2026-07-27 | riga fondativa: la PR che crea metro e procedura (guardie, modelli, skills, prompt snelliti) | baseline da stabilire | baseline da stabilire | baseline da stabilire | passa, con riserva dichiarata: il metro non puo' precedere se stesso. In luogo delle eval, un giro end-to-end dispatcher -> scout senza intervento (run `scout-20260727T203950Z-6f47`, cancello verde, 5 triage motivati) e il self-test del metro. Il PRIMO giro pieno di eval fissa la baseline, e da li' in poi la regola vale intera. |
| 2026-07-28 | routing e scoperta, non giudizio: `--priority` acceso nel comando del dispatcher, scout senza tetto sulle proposte piu' `--refresh` per ri-sondare il catalogo | n/a | n/a | n/a | passa. Il cambio non tocca modello ne prompt di giudizio di writer/reviewer/verificatore, quindi le tre eval non si applicano (misurerebbero cio' che non cambia); il metro resta comunque integro (`score_eval.py --self-test` ok). La prova per un cambio di routing e' quella della riga fondativa: comportamento candidato deterministico verificato: senza `--priority` il dispatcher instrada allo scout (ordine di catena, 50 in coda), con `--priority` al reviewer (smentita pubblica aperta, priorita' 120 > soglia 100). L'uncap dello scout e' codice, coperto da `tests/unit/test_scout_sources.py` (Uncapped) e dalla suite intera. |
| 2026-07-28 | merge/meccanica, non giudizio: ogni stadio della catena passa da `checks` a `auto` (`MERGE_POLICY`), piu' la robustezza scout (retry 5xx del catalogo SDMX, `REGIONAL_HINT` riconosce `reg.`). Prosa degli agenti riallineata (scout/hunter/curator/verificatore: "merge mode is auto"). | n/a | n/a | n/a | passa. Non tocca il giudizio editoriale di writer/reviewer/verificatore ne' le rubriche: le tre eval misurerebbero cio' che non cambia. Metro integro (`score_eval.py --self-test` ok). Comportamento candidato verificato deterministicamente dalla suite: un verdetto verde di curator/scout/... ritorna `merge: auto` (`test_no_chain_stage_waits_on_the_remote_ci`, `test_a_green_curator_merges_on_the_local_gate`), `decide()` con verdetto `auto` fonde senza aspettare i check (`test_it_merges_without_looking_at_the_checks`), e il meccanismo `checks` resta testato per il giorno in cui la CI partisse sulle PR MCP. Suite intera verde (840 test). Motivo: `checks` aspettava una CI che non parte sulle PR via MCP, quindi comprava un deadlock, non un verdetto; il cancello locale (stessa suite, stesso perimetro) gira prima del merge. |
| 2026-07-28 | **BASELINE stabilita**: primo giro pieno delle tre eval sugli agenti attuali (nessun cambio candidato, si misura lo stato di oggi). | ok (0 cifre fuori brief, 0 caratteri vietati) | 7/7 errori piantati trovati | 12/12 accuratezza, precision 1.0, recall 1.0 sulle smentite | La riserva della riga fondativa e' sciolta: da qui in poi ogni cambio di modello/prompt/rubrica si confronta con questi numeri, e un punteggio che scende e' un no. Metodo (da replicare identico su ogni candidato): fixture congelate, agenti fatti girare su **opus** con le loro istruzioni reali (`.claude/agents/*.md` + STYLE + rubrica + skill `indicator-review`), in una sessione **senza** l'hook di perimetro per-stadio, perche' `evals/out/` e' fuori dal perimetro di ogni stadio. Nota: gli agenti fanno cappotto sul set, quindi il metro oggi non distingue miglioramenti al soffitto ne' regressioni piccole: allargare il canary set (piu' casi difficili, punteggi non pieni) e' il prossimo lavoro sul metro. Attrito da chiudere: una deroga `evals/out/` in `scripts/agent_guard.py` per poter girare l'eval con l'agente hooked vero. |
| 2026-07-28 | **PRODUTTORE** (nuovo agente `producer.md`, fonde curator+writer+reviewer con rilettura reflexion). Candidato, contro la baseline, prima di renderlo default editoriale. | ok (0 cifre fuori brief, 0 caratteri vietati) | vedi nota: la rilettura sul proprio testo | vedi nota | **passa, >= baseline.** Sul compito writer del canary il produttore fa `ok` come la baseline. Il punto e' il passo di rilettura: sul proprio testo ha preso e corretto le classi che il reviewer eval misura, `definizione` (denominatore sbagliato: forza lavoro invece di popolazione 15-64) e `causale`/`eco` (media del cruscotto + intervallo falso + causa inventata nel quadro), riscrivendo verso mediana, scala umana e co-occorrenza con confondente ed eccezione. Autovalutato 18/20 (il punto in meno e' Fonti, WebSearch disabilitato in eval). Metodo identico alla baseline (opus, istruzioni reali, senza hook). Regge la fusione scrivi+rileggi: il produttore puo' diventare default editoriale. Resta vero il limite del metro (al soffitto: allargare il set con casi non pieni misurerebbe meglio la rilettura). |
| 2026-07-28 | **Ri-architettura a tre ruoli + monitoraggio + learning loop.** Ritiro dispatcher -> lanciatore per-indicatore (`pipeline_launch.py`); rotta viva `/_pipeline` + `pipeline_monitor.py`; sezione ERRORI NOTI nel brief (`indicator_brief.py`); ritiro dei 5 agenti di stadio (fusi in produttore/ammissione). | n/a | n/a | n/a | **passa, il metro e' integro** (`score_eval.py --self-test` ok, ora anche l'ammissione). Nessuno di questi cambi tocca le fixture di giudizio congelate: le tre eval editoriali girano sul brief congelato (`evals/writer/brief_ter-178.txt`) e su articoli/affermazioni congelati, non sul `indicator_brief` vivo, quindi la sezione ERRORI NOTI (che ARRICCHISCE il brief vero con le smentite gia' confermate, un esempio negativo in piu') non puo' farle regredire: la misurerebbe solo un canary sul brief vivo, ed e' un miglioramento atteso, non una regressione. Il lanciatore, il monitoraggio e il ritiro degli agenti sono routing/osservabilita', non giudizio. Prova di comportamento: suite intera verde (839), il lanciatore instrada solo verso i 3 agenti vivi (admissions/producer/indicator-verifier), la board risponde 'dov'e' fermo e perche' dal vivo. La baseline delle tre eval (writer ok / reviewer 7/7 / verifier 12/12) e dell'ammissione (11/11) resta il riferimento per il prossimo cambio di modello o prompt. |
| 2026-07-29 | **meccanica/osservabilita', non giudizio**: isolamento delle run (worktree per run, `pipeline_workspace.py`), cancello `--committed-only` al merge, PR aperta via REST senza `GH_REPO` (`pipeline_merge.py --open`), cruscotto `/_pipeline` vivo (battiti e PR aperte via POST -> SQLite/GCS). Prosa aggiornata: `AGENT_CONTRACT.md`, `pipeline-close-run/SKILL.md`, `launcher.md`, `rules/pipeline.md`. | n/a | n/a | n/a | **passa, il metro e' integro** (`score_eval.py --self-test` ok). Nessuno di questi cambi tocca il giudizio editoriale (rubrica, STYLE, classi d'errore, criteri di triage): le fixture congelate misurerebbero cio' che non cambia, come per le righe di routing/meccanica del 28 luglio. Il cambio ai prompt e' procedurale: **come** una run apre (worktree) e chiude (PR via REST), non **cosa** giudica. Prova di comportamento: suite intera verde (870 test), piu' i nuovi unit test `test_pipeline_workspace` (sequenza fetch->worktree, unicita' branch per run_id), `test_pipeline_inflight` (classificazione CI, slug senza GH_REPO), `test_pipeline_state` (freschezza dei battiti), `test_pipeline_merge::TheChainOpensPullRequestsOverRest` (PR via REST), e i due test EUR ricostruiti su fixture sintetica. La baseline delle quattro eval resta il riferimento per il prossimo cambio di modello o prompt di giudizio. |
| 2026-07-28 | **AMMISSIONE: nuova eval + baseline.** Aggiunta la quarta eval congelata (`evals/admissions/cases.json`, 11 casi di triage, scorer `score_admissions` con self-test), perche' l'ammissione fonde scout+hunter+promoter ed era l'unico ruolo di giudizio senza metro. Poi baseline dell'agente `admissions.md` su di essa. | n/a (compito editoriale, non tocca lo scrittore) | n/a | n/a | **baseline ammissione fissata: 11/11, precision approvati 1.0, recall approvati 1.0, zero false approvazioni.** Metodo identico alla baseline delle tre (opus, istruzioni reali di `.claude/agents/admissions.md`, senza l'hook di perimetro perche' `evals/out/` e' fuori dal perimetro d'ogni stadio). I casi coprono apposta le classi che feriscono: aggregatore privato non istituzionale (a02), duplicato di serie gia' nel catalogo (a04, a09), solo nazionale senza grana territoriale (a05), licenza non nominabile (a03), copertura rada sotto il pavimento (a07), verso indifendibile da portare a contextual (a08), anno di punta provvisorio `p` (a10). L'agente distingue i tre esiti col taglio giusto (respinto = difetto strutturale del dato, needs-info = fatto verificabile mancante). Metro integro (`score_eval.py --self-test` ok, e prova che un 'approva tutto' fa crollare la precision). Da qui un cambio di modello/prompt dell'ammissione si confronta con questi numeri. Limite noto, uguale alle altre: l'agente fa cappotto sul set, quindi il metro non vede regressioni piccole finche' il set non si allarga con casi a punteggio non pieno. |
| 2026-07-29 | **modello: agenti di giudizio da `opus` (alias, oggi Opus 5) a `claude-opus-4-8`** (candidato, per ridurre il consumo). Misurato per ruolo, applicato solo dove regge. | **fallisce**: 1 cifra fuori brief (`55`, da "poco sotto il 55%", l'arrotondamento della media 54,87 che nel brief non compare). Baseline era `ok` (0 cifre fuori brief). | 7/7 (nessun errore piantato sopravvive) | 12/12 accuratezza, precision/recall 1.0 sulle smentite | **passa parziale, applicato solo ai ruoli che reggono.** `indicator-verifier` (verifier 12/12, come baseline) e `admissions` (11/11, precision approvati 1.0, zero false approvazioni, come baseline) promossi a `claude-opus-4-8`. Il `producer` resta su `opus`: fonde scrivi+rileggi, e sul ramo scrittura il candidato regredisce (introduce `55%`, arrotondamento non nel brief, cioe' la classe "numero che non viene da nessuna parte" che questa eval esiste per prendere), anche se la rilettura regge (reviewer 7/7). Metodo: agenti veri (`.claude/agents/*.md`), fixture congelate, brief congelato, senza dati vivi ne' web, una eval per ruolo. Metro integro (`score_eval.py --self-test` ok). Caveat di misura dichiarato: in questo ambiente il modello effettivo di un subagent non e' verificabile in modo indipendente (la riga d'identita' e' fissata a livello di sessione), quindi l'attribuzione "su 4.8" poggia sul rispetto del `model` nel frontmatter, non su una lettura del routing. I punteggi deterministici valgono comunque, qualunque fosse il modello. |
| 2026-07-29 | **meccanica/osservabilita', non giudizio**: il lanciatore (`.claude/agents/launcher.md`) registra i `subagent_tokens` di ogni ruolo che lancia, POSTandoli a `/_pipeline/beat` (azione `tokens`, telemetria durevole in SQLite/GCS) chiavati sul run_id del ruolo, cosi' il cruscotto mostra il consumo token per step e il totale per indicatore. | n/a | n/a | n/a | **passa, il metro e' integro** (`score_eval.py --self-test` ok). Il cambio al prompt del lanciatore e' puro routing/telemetria: non tocca il giudizio editoriale (rubrica, STYLE, classi d'errore, triage), quindi le fixture congelate misurerebbero cio' che non cambia, come le righe meccaniche del 28-29 lug. Il lanciatore non scrive articoli ne' verdetti: aggiunge una riga di telemetria a fine dei ruoli che gia' lanciava. Prova di comportamento: unit `test_pipeline_monitor::PostTokens` (payload `tokens` giusto, silenzio senza ambiente) e integrazione `test_pipeline_dashboard_route` (l'azione `tokens` scrive col segreto e 404 senza, il token e' durevole oltre la finestra dei battiti, e compare per step sul cruscotto). La baseline delle quattro eval resta il riferimento per il prossimo cambio di modello o prompt di giudizio. |
| 2026-07-31 | **configurazione del picker, non giudizio**: aggiunta a `.claude/settings.json` la voce personalizzata `claude-opus-4-8`, visibile come `Opus 4.8` nel menu `/model`. | n/a | n/a | n/a | **passa, il metro e' integro** (`score_eval.py --self-test` ok). La voce non cambia il modello predefinito, gli alias o il frontmatter degli agenti e non modifica quindi alcun giudizio editoriale. JSON valido, `git diff --check` verde e configurazione accettata da Claude Code 2.1.218 tramite `claude doctor`. |
| 2026-08-01 | **PRODUTTORE: skill `scrittura-italiana` agganciata in scrittura/rilettura** (Fase 5), con precedenza ASSOLUTA degli assoluti di progetto (`content/STYLE.md`: mai `— – ; …` ne' caporali). Cambio di prompt+skill di un ruolo di giudizio, misurato prima di applicarlo. Modello invariato (`opus`). | **ok** (0 cifre fuori brief, 0 caratteri vietati; `tic_count` 0/1000, burstiness 0,52), come la baseline | **7/7** errori piantati trovati, come la baseline | n/a (il verificatore non e' toccato: la skill entra solo nel produttore) | **passa, >= baseline.** Metodo identico alla baseline (subagent con le istruzioni reali di `producer.md` + la skill candidata, brief e fixture congelati, senza dati vivi ne' web, senza hook di perimetro). La skill non regredisce il ramo cifre (il rischio numero uno di un humanizer: arrotondare o inventare un numero): il contratto di conservazione della skill coincide con quello della catena. Zero caratteri vietati: la precedenza tiene, il registro *testo controllato* della skill (caporali, `;`, lineette) non passa. Metro integro (`score_eval.py --self-test` ok). La misura estesa sta nel confronto prima/dopo di `evals/scrittura-italiana/CONFRONTO-PROSA-2026-08-01.md` (valore marginale nullo sul produttore gia' completo, alto sul backlog e sui blog). Applicato: la skill agganciata al produttore serve il roll-out sul backlog via Routine. |
| 2026-08-01 | **routing/meccanica, non giudizio**: cap di parallelismo del lanciatore (`pipeline_launch.py --max-parallel`, default 3). Il piano offre al massimo tre voci per tick, gia' ordinate per priorita' (una smentita pubblica, peso 100, non viene mai tagliata). Prosa di `launcher.md` aggiornata. | n/a | n/a | n/a | **passa, il metro e' integro** (`score_eval.py --self-test` ok). Non tocca il giudizio editoriale (rubrica, STYLE, classi d'errore, triage): le fixture congelate misurerebbero cio' che non cambia, come le righe meccaniche del 28-29 luglio. Il cambio governa **quanti** ruoli partono per tick, non **cosa** giudicano. Prova di comportamento: unit `test_pipeline_launch::TheParallelismCap` (cap di default 3, taglia la coda non la testa, `--max-parallel 0` non taglia, `--top` override), suite verde. Motivo: la richiesta esplicita dell'utente di non far partire dieci subagent insieme. |
| 2026-08-03 | **hook, non giudizio**: `scripts/agent_guard.py` rifiutava l'unico idioma sanzionato per aprire una PR (`AGENT_CONTRACT.md`/`pipeline-close-run/SKILL.md`, `NOME=$(python3 scripts/pipeline_merge.py --open ...)`), per un bug di parsing: `re.split` spaccava il comando anche dentro le virgolette (un `--body "a; b"` o un `-m "riga\n\npar"` si spezzavano a meta') e lo spogliatore di assegnazioni env faceva match su `NOME=$(python3` come assegnazione semplice, scartando anche il comando vero. Riscritto lo split di primo livello (quote-aware, carattere per carattere) e riconosciuto `NOME=$(...)` come caso a se', validato ricorsivamente. Piu': `pipeline_log.py --write` ora rifiuta `--run-id` mancante invece di coniarne uno in silenzio (`--mint-run-id` per chi non apre mai una PR), e `dump_indicator_stats.py --help` non riscrive piu' la fixture congelata. | n/a | n/a | n/a | **passa, il metro e' integro** (`score_eval.py --self-test` ok). Nessuno dei tre cambi tocca il giudizio editoriale (rubrica, STYLE, classi d'errore, triage): le fixture congelate misurerebbero cio' che non cambia, come le righe meccaniche di luglio-agosto. Prova di comportamento: le tre riproduzioni empiriche del bug (newline in `-m`, `;` in `--body`, `NOME=$(...)`) ora passano la guardia, `DENY_PATTERNS` continua a bloccare `rm -rf`/`git push --force`/`gh pr merge` sugli stessi input e un secondo comando reale su una riga nuova resta giudicato a se' (non e' un allentamento della guardia, solo la correzione del suo parsing); suite intera verde (982 test, +7 unit `test_agent_guard`, +1 unit `test_dump_indicator_stats`, +2 integration `test_pipeline_log`). Occasione anche per una revisione della catena non legata a un bug puntuale: la chiusura di una run faceva girare la suite intera tre volte (manuale, cancello, merge) senza un motivo scritto per la prima; il passo manuale della skill `pipeline-close-run` ora gira solo `tests/unit` (<1s), il cancello e il merge restano invariati (entrambi hanno un motivo indipendente: il primo iterando il catalogo intero, il secondo per non fidarsi del verdetto auto-riportato). |
| 2026-08-04 | **RUBRICA + STYLE + PRODUTTORE: il criterio 8 da `Ritmo e imperfezione` a `Leggibilita'`**, piu' una libreria nuova di dieci modelli di registro (`content/esempi/`) che il produttore legge prima di scrivere e contro cui si rilegge prima di firmare. Cambio di giudizio editoriale vero, non di meccanica. Modello invariato (`opus`). | **ok** (`cifre_fuori_brief: []`, `problemi: []`), come la baseline | **7/7** errori piantati trovati, come la baseline | n/a (il verificatore non e' toccato: cambiano rubrica, stile e prompt del produttore) | **passa, pari alla baseline sul rigore.** Origine: un utente ha giudicato `ter-167` "macchinoso, si vede che e' tradotto in inglese" e ha allegato una riscrittura piu' leggibile. Misurando, la diagnosi ovvia e' caduta: la riscrittura preferita pesa **12,5 tic/1000** contro i **2,2** dell'articolo pubblicato, burstiness 0,28 contro 0,46, e il criterio 8 vecchio l'avrebbe percio' bocciata. Il difetto non era l'esecuzione ma il **bersaglio**: il produttore centrava la varieta' di ritmo che gli era scritta, e il difetto vero era l'**impilamento** (tre idee in una frase, cinque virgole). Controprova indipendente dalla libreria nuova: i due estratti Openpolis pesano 21,4 e 18,9 tic/1000, dieci volte l'articolo illeggibile, e si leggono senza inciampi. Percio' `tic_count.py` **non e' stato toccato**: le otto categorie restano, `mente` e `gerundite` diventano indicative sotto il bersaglio nuovo, `burstiness` neutra, e il campo `vietati` resta l'unico cancello duro. Il rigore non e' cambiato di una riga (brief unica fonte, `definition_check`, classi d'errore, fonti verificate, firma con `reviewed_vintage`), ed e' quello che le due eval provano. **Che cosa NON provano:** contano cifre fuori dal brief, caratteri vietati ed errori piantati, quindi non vedono la leggibilita'. Quella e' misurata a parte, col confronto cieco a due giudici di [`evals/scrittura-italiana/CONFRONTO-LEGGIBILITA-2026-08-04.md`](../evals/scrittura-italiana/CONFRONTO-LEGGIBILITA-2026-08-04.md), che porta anche il testo del writer eval misurato contro `ter-167` (16,1 parole per frase contro 21,7, virgole medie 1,06 contro 1,49). Metro integro (`score_eval.py --self-test` ok). Metodo identico alla baseline (subagent opus, istruzioni reali, fixture e brief congelati, niente dati vivi ne' web, senza hook di perimetro). **Onesta' sulla misura:** la precedenza della leggibilita' sulla banda 500-700 parole e' stata scritta in `producer.md` DOPO il giro writer, in risposta a un attrito che l'agente stesso ha segnalato (era finito a 737 parole). Non puo' muovere il punteggio, che conta cifre e caratteri, ma il prompt misurato non e' identico a quello fuso. **Il confronto cieco, due giudici opus con assegnazione invertita fra loro:** sul vincitore non concordano (17-14 per il pubblicato, 18-17 per il rigenerato), sulla diagnosi si'. **Il criterio 8 da' zero al pubblicato da entrambi**, e i due citano gli stessi tre periodi impilati come prova senza essersi parlati; il rigenerato prende 1 e 2. Il pavimento anti-singhiozzo morde: il giudice 1 ha dato 1 e non 2 al testo nuovo per un paragrafo "che non nasce da quello prima", cioe' la clausola aggiunta alla colonna dello zero, applicata contro il testo nuovo senza suggerimento. **Applicazione limitata di proposito:** le istruzioni nuove valgono da subito per gli articoli **nuovi**, e la coda dei 33 articoli completi del produttore **resta ferma**, perche' rigenerare oggi una serie lunga baratta leggibilita' contro contenuto (vedi sotto), e per un giudice su due il baratto e' in perdita. Nessun articolo si invalida a sorpresa: il rientro e' guidato da `reviewed_vintage` contro `vintage`, non da un cambio di rubrica. **Il bloccante, scoperto in questo giro e non causato da questo cambio:** il blocco `ANDAMENTO` di `scripts/indicator_brief.py` (righe 549-562) emette la media del primo e dell'ultimo anno, la variazione, il trend del divario e i delta per territorio, ma **non la serie annuale della media**. Il picco 2007, il fondo 15,17 del 2014 e i nove anni sotto il 1995 non sono percio' derivabili dal brief, e la regola "il brief e' la sola fonte dei numeri" li vieta. L'articolo pubblicato li contiene, il che vuol dire che sono stati presi altrove e che il verificatore li ha confermati lavorando contro i dati vivi: non un errore di fatto, un buco nel perimetro. La correzione e' una manciata di righe in `indicator_brief.py` piu' un canary suo, perche' arricchire il brief cambia cio' che un articolo puo' dire. Altri attriti preesistenti registrati nel confronto e non chiusi qui: la classe `universale` non copre le affermazioni sulla forma della distribuzione, un falso aritmetico contro la serie non ha classe, `prose_lint --show` e' irraggiungibile in eval, il brief stampa gli `h2` vecchi e riusarli vanifica la misura in silenzio, l'autovalutazione sui dieci criteri satura (l'agente writer si e' dato 20/20). Difetto reale trovato per caso sulla pagina viva: in `content/indicators/167.json` il paragrafo della cautela sul nome usa apostrofi ASCII (`puo'`, `e'`, `intensita'`) mentre il resto dell'articolo usa gli accenti veri. |
| 2026-08-04 | **prosa, non giudizio**: allineata la prosa di due agenti alla ratifica **merge = pubblicazione** (PR #142, che ha rimosso lo stato `fusa` e la verifica-sito). `launcher.md`: il comando resta `--publish` ma la prosa ora descrive il battito del lanciatore nel diario, non una verifica del sito (tolto anche `--publish-base`, inerte). `producer.md`: una frase che citava `scripts/verify_publication.py` e il passo `fusa -> pubblicata` a valle del deploy, ora "il merge e' gia' la pubblicazione". | n/a | n/a | n/a | **passa, il metro e' integro** (`score_eval.py --self-test` ok). Il cambio non tocca il giudizio editoriale (rubrica, STYLE, classi d'errore, criteri di triage): le fixture congelate misurerebbero cio' che non cambia, come le righe di prosa procedurale del 28-29 luglio e 1 agosto (telemetria e cap del lanciatore). E' una **correzione fattuale** di prosa gia' resa falsa da un cambio di codice gia' fuso e testato (PR #142, suite verde a 953): descrive **cosa fa** oggi `--publish` (segna il diario) e **cosa non c'e' piu'** (il passo del sito), non **cosa giudica** il produttore. Nessun cambio a modello, hook, permessi o rubrica. La baseline delle quattro eval resta il riferimento per il prossimo cambio di modello o prompt di giudizio. |
| 2026-08-06 | **BRIEF: la serie annuale della media** (`scripts/indicator_brief.py`, blocco `ANDAMENTO`). Il brief e' la sola fonte dei numeri per il produttore, e finora emetteva solo la media del primo e dell'ultimo anno: un picco o un fondo interno alla serie (il 2007 e il fondo 15,17 del 2014 di `ter-167`) non erano derivabili dal brief, quindi rigenerare oggi un articolo lungo per leggibilita' barattava leggibilita' contro contenuto. Ora emette la media semplice sulle regioni per ogni anno della matrice, **ciascuna con la sua base territoriale**. Il bloccante segnalato nel canary del 4 agosto. | n/a | n/a | n/a | **passa, il metro e' integro** (`score_eval.py --self-test` ok). **Arricchimento del brief VIVO, non del giudizio**: le quattro eval girano contro fixture congelate (`evals/writer/brief_ter-178.txt`, articoli e affermazioni congelati), non contro il brief vivo, quindi non possono ne' regredire ne' vedere questo cambio, esattamente come la riga ERRORI NOTI del 28 luglio (un altro arricchimento del brief). E' un miglioramento atteso, non una regressione: da' al produttore piu' fonte, non un giudizio diverso. **Verifica deterministica** (unica fonte, nessuna contraddizione): la media annua e' la stessa media semplice sulle regioni che `stats` gia' calcola, provato combaciando agli estremi (`year_min`/`year_max` == `year_min_avg`/`year_avg` a 1e-6 su `ter-167`), e il fondo interno riappare (2014 = 15,17, la cifra che il canary del 4 agosto diceva non derivabile). Ignora le celle vuote come `data.indicator_year_average`. Guardia sul caso corto: una serie a due anni o meno non stampa il blocco. **Guardia sulla base territoriale, aggiunta dopo la revisione Codex (P1) e la sola parte di questo cambio che tocca cio' che un articolo puo' dire:** la copertura di una matrice cambia da un anno all'altro, e senza denominatore la serie invitava a chiamare "fondo" un anno in cui a muoversi era il campione. Il caso reale che lo prova e' il BES provinciale `06POL001P`, 50 province nel 2020 e 5 nel 2021, media da 58,52 a 43,30. Ora ogni anno porta il suo `n`, e la forma la sceglie una regola meccanica invece di una soglia inventata (**i conteggi sono tutti uguali oppure no**): su base costante, il caso comune delle venti regioni, la base si dichiara una volta in testa e le celle restano pulite (provato su `ter-167`, nessun rumore aggiunto al caso comune); su base variabile ogni cella porta il suo `n` e la serie si apre dicendo che due anni non sono confrontabili. Lo stesso richiamo e' esteso alla riga `media primo -> ultimo anno`, che aveva il difetto gia' prima di questa PR. Allineato a `docs/INDICATOR_PAGES.md` sul trend di lungo periodo, che chiede gli anni, l'unita' **e la base territoriale**. **Secondo giro di revisione, tre correzioni:** (a) **la base non e' il conteggio, sono i territori.** Conteggi uguali non fanno una base uguale: `bes-06POL007` ha 19 regioni nel 2012 e 19 nel 2025, ma nel 2012 c'e' il Veneto e non la Calabria e nel 2025 il contrario, e il controllo sui soli numeri dichiarava confrontabili due medie che non lo sono. Ora ogni anno porta **quali** territori rispondono e i confronti sono fra insiemi, con la riga primo-ultimo anno che nomina chi entra e chi esce. (b) `--json` non emetteva `annual_means`, quindi il consumatore macchina non riceveva la funzione di questa PR: ora la riceve, base territoriale compresa. (c) Le sei prove nuove sono viste pure su matrici sintetiche e stanno in `tests/unit/test_indicator_brief.py`, non fra le integration: e' il contratto di `CLAUDE.md` sulla divisione della suite, e messe fra le lente la suite veloce dello sviluppo non le vedeva. **Terzo giro, una:** (d) **la variazione fra gli estremi non si annota, si sostituisce.** Con basi diverse ai due estremi la riga stampava comunque `media 2012 -> 2025 (12,45 punti)` e ci appendeva l'avvertimento: una sottrazione fra popolazioni diverse, precisa e non valida, cioe' la cifra piu' pericolosa che un brief possa stampare perche' ha l'aria di essere derivata e un articolo la cita per prima. Ora quella riga non c'e', e al suo posto va il confronto sui soli territori presenti in **entrambi** gli anni (`endpoints_common`, media e divario), che vuol dire qualcosa. Con intersezione vuota il brief dice che il confronto di lungo periodo non si puo' fare, invece di stampare un numero. Idem per il trend del divario, che aveva lo stesso difetto. Suite intera verde (988 test, +10 unit: base costante silenziosa, base variabile che avverte, conteggio uguale ma territori diversi, celle vuote che non contano zero, serie corta muta). **Cosa NON prova:** la misura di testa (che il produttore usi la serie senza inventare) e' un canary sul brief VIVO, che si fara' quando il produttore rigenerera' i 33 articoli completi; il rigore deterministico prova che il brief non si contraddice e non introduce una cifra che non viene da nessuna parte. |
| 2026-08-06 | **READER-EDITOR: nuovo ruolo, quinto giudizio della catena.** Un critico indipendente a valle del produttore, in parallelo al verificatore ma su un altro asse: il verificatore misura se i fatti reggono, il reader-editor se un lettore comune capisce la pagina al primo passaggio. Read-only (niente Edit, niente web), `soft` (accoda, non blocca il merge). Nuova coda `scripts/reading_queue.py` + store `data/pipeline/letture/` (append-only, protetto dal cancello come le verifiche); agente `.claude/agents/reader-editor.md` (opus); perimetro `pipeline_gate.STAGE_PATHS[reader-editor]=(letture/, runs/)`, non firma; due flag `leggibilita_grave`/`leggibilita` in `review_queue`; aggancio quarta lista in `pipeline_launch`. Ruolo NUOVO: bootstrap di baseline, come l'ammissione, non confronto con una precedente. | n/a (nessun cambio ai prompt/rubrica di writer/reviewer/producer) | n/a | n/a (il verificatore non e' toccato) | **baseline reader-editor fissata: 8/8, precision_revise 1.0, recall_revise 1.0, zero falsi pass.** Nuova eval `evals/run_eval.py reader-editor` + gold set `evals/reader-editor/cases.json` (4 pass reali + 4 revise: `eur-rd_p_persreg` il caso guida, `ter-167` che un utente ha giudicato "macchinoso", piu' due costruiti a fallimento duro). Metro integro con la nuova eval inclusa (`score_eval.py --self-test` ok, e prova che 'promuovi tutto' fa crollare il recall e 'boccia tutto' la precision). Metodo identico alle altre baseline: subagent **opus** con le istruzioni reali di `reader-editor.md`, fixture congelata, niente dati vivi ne' web, senza l'hook di perimetro. **Le quattro baseline vecchie non si applicano e non possono regredire:** questo slice non tocca modello, prompt, rubrica o hook di writer/reviewer/producer/verificatore/admissions. `review_queue` (i due flag) e' meccanica di ordine-di-lettura, non giudizio; il flag `leggibilita` e' un asse separato dai sospetti di falsita' (peso `leggibilita_grave`=46 sotto `smentita`=60 e `definizione`=50, "misinformare batte confondere"; `leggibilita`=22 sopra i cosmetici), e come `smentita` invalida la firma cosi' il produttore riscrive. L'aggancio al lanciatore e' routing (quarta lista, i 300 pubblicati restano `pubblicata`, con uno slot riservato in `cap_for_tick` perche' le letture ad alta priorita' non monopolizzino il tick: l'arretrato drena a una voce a tick, il lavoro fattuale tiene gli altri due slot). Suite intera verde (1024 test, +19 `test_reading_queue`, +10 gate integration, +4 `test_review_queue`, +7 `test_pipeline_launch`, +1 gruppo monitor). **Due correzioni dalla revisione Codex, entrambe di meccanica e nessuna di giudizio, quindi il metro resta quello di questa riga** (`score_eval.py --self-test` ok dopo il cambio): (1) **P1, ambito della run.** Le istruzioni dicevano di lavorare un lotto di cinque-dieci articoli mentre il lanciatore passa un indicatore per voce e puo' aprire piu' letture nello stesso tick: due sessioni avrebbero scelto gli stessi articoli dalla coda e scritto lo stesso file, con conflitti di merge e giudizi doppi. Ora `reader-editor.md` legge **solo** l'indicatore che gli arriva (una run, un articolo, un file), `launcher.md` ha la voce `role: reader-editor` che gliene passa uno, e la regola per-indicatore in `docs/AUTONOMOUS_PIPELINE.md` nomina anche lui. `cap_for_tick` non e' toccato: con i bersagli distinti tre letture in parallelo scrivono nomi diversi, e la collisione sparisce nel prompt invece che nel cap. Cambio di **perimetro di lavoro**, non di criterio: gli otto criteri, i fallimenti duri e la soglia non si muovono di una riga, quindi la baseline 8/8 di questa riga resta il confronto valido. (2) **P2, cancello.** Il caricatore delle schegge saltava in silenzio un file troncato o non-oggetto: una run che ne aggiungeva uno solo arrivava al cancello senza righe, il cancello diceva verde "nessuna lettura nuova" e la scheggia veniva fusa lo stesso, illeggibile per la coda che continuava a rilanciare l'articolo. Ora i file illeggibili aggiunti bocciano, e il controllo viene **prima** dell'uscita "nessuna lettura nuova". Corretto anche il gemello `data/pipeline/verifiche/`, che aveva lo stesso buco per copia e sarebbe rimasto l'unico a tenerselo (le due letture passano da un solo `_rows_added`). **Secondo giro di revisione, altre due, sempre meccanica:** (3) **P1, la riscrittura partiva cieca.** `build_queue` buttava via la `note` e gli otto punteggi, e il motivo del lancio diceva solo "bocciato per leggibilita'": il produttore sapeva di essere caduto e non dove, quindi poteva rifare la meta' sbagliata dell'articolo e farsi bocciare di nuovo fino al parcheggio del freno. Ora la riga di coda porta `note` e `low_scores` (i criteri sotto il massimo, dal piu' basso), il motivo del lancio li scrive in ordine di durezza (fallimenti duri, criteri, punto d'inciampo), e `launcher.md` dice di passare il `reason` testuale al produttore. Nessuna riga di `producer.md` toccata: il canale e' il lancio, non un prompt di giudizio nuovo. (4) **P2, verifica e lettura sullo stesso indicatore.** Un articolo appena firmato sta insieme fra i verificabili e fra i non letti, e le due liste emettevano due voci che il lanciatore metteva in volo insieme: `ROLE_ORDER` non lo impediva, e' solo il criterio che rompe i pari merito nell'ordinamento. Se la lettura boccia, la riscrittura fa scadere la verifica appena fatta. Misurato sul piano vero: **23 indicatori doppi su 425 voci**, e il tick in cima lanciava i verificatori di `dem:GROWTHRATEN` e `dem:GROWTHRATET` mentre le loro letture restavano sotto il taglio. Ora `_one_role_per_indicator` tiene, per indicatore, il primo ruolo di `ROLE_ORDER` e gli lascia la priorita' piu' alta fra quelle in collisione (425 voci -> 402, zero doppioni). Non e' lavoro perso ma rimandato: il piano si ricalcola a ogni tick e la verifica ricompare appena la lettura esiste. **Il prezzo, dichiarato:** 23 delle 28 verifiche pronte aspettano la loro lettura, che drena a una per tick, quindi il verificatore rallenta finche' l'arretrato di lettura non si sgonfia. E' l'ordine che questa PR dichiara di volere ("leggi prima, verifica dopo"), pagato una volta invece di pagare ogni volta una verifica buttata. Suite intera verde (1029 test). **Terzo giro, una sola e sempre meccanica:** (5) **la nota non era controllata.** Il canale aperto al punto (3) restava opzionale: un `revise` con nota vuota passava il cancello e ricreava la riscrittura cieca, e una nota scritta come lista (`"note": ["..."]`, l'errore piu' facile in un JSON a mano) sarebbe passata pure, per poi alzare un `AttributeError` sul `.strip()` di `build_queue` **dopo** il merge, fermando coda, lanciatore e coda del revisore per il catalogo intero. Ora `row_problems` pretende che la nota sia una stringa sempre, e non vuota su un `revise`, e `build_queue` la legge difensivamente (il cancello guarda le schegge aggiunte, la coda gira anche su cio' che e' gia' fuso). Suite intera verde (1032 test). **Quarto giro, il gemello della nota sull'altro campo:** (6) `hard_failures` non era controllato nel tipo. `[["numeric_overload"]]` e' una lista non vuota, quindi passava il controllo di coerenza, e poi l'elemento interno finiva nel `", ".join(...)` del motivo di lancio e di `review_queue`, con un TypeError dopo il merge che ferma lanciatore e coda del revisore per il catalogo intero. Le sette classi diventano un vocabolario chiuso in `HARD_FAILURES` (le stesse di `reader-editor.md`, con un test che le tiene allineate al prompt): non sono prosa, sono etichette che un altro stadio legge, e una classe inventata da una run non direbbe niente a chi la riceve. Il cancello guarda il campo **grezzo**, perche' il lettore difensivo scarta in silenzio cio' che non e' stringa e un controllo sul ripulito accetterebbe proprio la riga storta che deve fermare. Suite intera verde (1036 test). **Quinto giro, e tocca la misura stessa:** (7) **la fixture regalava ancora la risposta, per la seconda volta.** Tolti `label`, `why`, `code` e il blocco `_`, restavano gli **id**: `p01`-`p04` sono i quattro `pass` e `r01`-`rc04` i quattro `revise`, quindi l'8/8 di questa baseline era raggiungibile leggendo il prefisso invece della prosa, e una baseline centrabile cosi' non vede piu' nessuna regressione. Ora `prepare()` sostituisce gli id con `score_eval.blind_id` (derivato dall'id del gold, non sorteggiato: nessun file di corrispondenza accanto alla fixture, che sarebbe la stessa fuga con un passaggio in piu') e ordina i casi per id cieco, perche' i quattro `pass` in fila sarebbero la risposta travestita da posizione. Lo scorer ricalcola la mappa e accetta anche gli id vecchi, cosi' **la baseline gia' annotata qui resta valida e confrontabile**: cambia cio' che l'agente vede, non cio' che il metro misura. Cinque prove nuove in `tests/unit/test_eval_blinding.py`. Suite intera verde (1041 test). **Onesta' sulla misura, per il prossimo giro:** l'8/8 registrato in questa riga e' stato ottenuto **prima** dell'accecamento, quindi non e' piu' provato che sia stato ottenuto leggendo la prosa. Non e' un punteggio da rialzare a mano: la prima eval del reader-editor girata sulla fixture cieca stabilira' la baseline vera, e questa riga va letta come il limite superiore. **Due onesta' sulla misura:** (1) la prima versione del gold nominava i revise nel blocco `_`, e `prepare()` non lo strippava: il fixture regalava le risposte. Trovato dal subagent stesso, corretto (`prepare()` toglie `_`, `label` e `code`; il `code` perche' `eur-rd_p_persreg` e' nominato come esempio-revise nelle istruzioni), e il **giro pulito** (fixture con solo `id`+`prose`) resta 8/8. (2) La run e' girata via subagent `claude` con le istruzioni reali, NON via il tipo-agente `reader-editor` hookato, perche' la sua guardia di perimetro blocca la scrittura in `evals/out/` (l'attrito gia' noto: servirebbe una deroga `evals/out/` in `agent_guard.py`). I punteggi deterministici valgono qualunque sia l'ospite. **Cosa la baseline NON prova:** il gold ha 8 casi netti, quindi il metro non vede regressioni piccole finche' il set non si allarga con casi a punteggio non pieno, lo stesso limite dichiarato per le altre eval. |
