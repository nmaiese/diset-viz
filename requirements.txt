# Versioni pinned, non range: la catena è non presidiata e la CI installa a
# ogni run, quindi "quello che pip risolve oggi" cambiava sotto i piedi senza
# che nessun file lo registrasse. Con il pin, un aggiornamento è un diff.
# Per aggiornare: alza la versione qui, fai girare la suite, committa.
Flask==3.1.3
Flask-Caching==2.4.1
Flask-Compress==1.24
gunicorn==23.0.0
Markdown==3.10.2
python-frontmatter==1.3.0
openpyxl==3.1.5
# Backend mutabile su Postgres (Supabase in produzione, SQLite in test/CI):
# stesso ORM sui due dialetti. psycopg v3 col driver `postgresql+psycopg`.
# Alembic possiede lo schema Postgres; PyJWT verifica i token Supabase (Fase 4).
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
# [crypto] tira dentro `cryptography`: senza, PyJWT NON verifica gli ES256/RS256
# (il JWT asimmetrico di Supabase), quindi ogni richiesta autenticata cadeva a
# 401 in produzione mentre in locale passava (cryptography già nel venv).
PyJWT[crypto]==2.10.1
